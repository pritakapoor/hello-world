#Prita Kapoor
#8010339
#12.9

list = input().split()
name = list[0]

while name != '-1':
    try:
        age = int(list[1]) + 1
    except ValueError:
        age = 0
    print('{} {}'.format(name, age))

    list = input().split()
    name = list[0]
